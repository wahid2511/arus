# DEPRECATED — do not install

This folder is the **legacy** HTTP localhost companion.

For the current Native Messaging extension, see:

- Source: [`../../extension/`](../../extension/)
- Chrome load unpacked: `extension/dist/chrome`
- Firefox: `extension/dist/firefox`

Do **not** load this folder in Chrome/Firefox.
