# Legacy — do not load unpacked

This folder is the **old HTTP localhost bridge** companion.

**Active companion:** [`../extension/`](../extension/) (Manifest V3 + Native Messaging)  
**Load in Chrome:** `../extension/dist/chrome` after `npm run build:extension:chrome`

Source files live under `arus/` for historical reference only. They are **not** packaged into the Electron app and must **not** be used for new installs.
